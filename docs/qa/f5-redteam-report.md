# Red Team F5 — Paridade CodexBar (Tasks 6–7, execução de 2026-09-10)

- **Escopo:** alert storm (fração oscilando 94↔96 no threshold 95), flip `resetsAt` nil↔data, settings corrompidas (thresholds fora de ordem, JSON lixo), providers novos com respostas hostis (fuzz fixture-replay), 50 contas, headers Alibaba em runtime, bypass programático de overlap residual, hermeticidade do e2e.
- **Evidência autoritativa:** suíte `./run-tests.sh` — `docs/qa/evidence/run-tests-2026-09-10-f5-final.log` (565/565, exit 0); E2E `docs/qa/evidence/e2e-f5-2026-09-10.log` (81 PASS, exit 0). Suítes: `RedTeamF5AlertTests`, `RedTeamF5SettingsTamperTests`, `RedTeamF5UITests`, `RedTeamF5CaptureGatewayTests` (Tests/TokenBarCoreTests e Tests/TokenBarUITests/RedTeamF5*), `RedTeamF5HostileProviderTests` (Tests/TokenBarProvidersTests).
- **Placar: 8/8 casos — 3 aceitos por contrato documentado, 1 P3 aceito, 3 fixados na fase, 1 P1 de gate fixado.**

| # | Caso | Resultado | Prova |
|---|------|-----------|-------|
| 1 | **Alert storm: fração oscilando 94↔96 no 95** | 🟡 ACEITO POR CONTRATO (rate limitado) | `RedTeamF5AlertTests.oscillationAroundThresholdRefiresPerCrossing`: 21 cruzamentos → exatos 21 disparos (1 por cruzamento, NUNCA 2 por avaliação); queda abaixo re-arma em silêncio; restart NÃO re-dispara (estado persistido). Sem histerese É o contrato da spec §8 — histerese seria política nova. Amortece: identifier de notificação substitui o banner da mesma causa (1 banner na tela) e o backoff do scheduler limita a cadência a 1 tentativa/60 s+ — pior caso real ≈ 1 notificação/minuto por (provider, conta, janela, threshold) durante oscilação sustentada. Documentado na Decisão 5 de `decisoes-f5.md` |
| 2 | **Flip `resetsAt` nil↔data (fração constante ≥ threshold)** | 🟡 ACEITO POR CONTRATO (1 disparo por flip) | `resetsAtFlipRefiresOncePerFlip`: nil→data, data→nil, nil→data → 1 disparo cada; mesmo `resetsAt` repetido → suprimido; o evento carrega o resetsAt DA VEZ. Semântica de renovação por definição (a chave de dedupe inclui o resetsAt) — flip é renovação do ponto de vista do motor; custo máximo 1 notificação por flip, banner substituído. Pino no mesmo teste |
| 3 | **Settings corrompidas** (thresholds `[95,50]`, JSON lixo) | 🟢 DEFENDE (defaults campo a campo) | `RedTeamF5SettingsTamperTests`: `[95,50]` persistido → `[50,95]` no apply E no `readConfig` (satura/ordena/remove duplicata; `[95,95,0,101,50]` → `[50,95]`); `alerts:enabled`/`thresholds`/`reminder`/`state` com lixo → defaults (OFF / [50,75,90,95] / OFF / estado vazio) e o engine avalia normalmente por cima; `scheduler:*` fora de faixa e `menubar:*` lixo → defaults (60/300/todos). Nunca crash, nunca valor intermediário inventado |
| 4 | **Providers novos com respostas hostis** (fuzz fixture-replay) | 🟢 DEFENDE (1 P3 aceito) | `RedTeamF5HostileProviderTests` (6 providers): JSON lixo → rethrow (decode do client); shape sem o bloco esperado → erro tipado (`OpenRouterAPIError`, `DeepSeekAPIError`, `GrokAPIError`, `AntigravityAPIError`); `1e308` → finito, sem overflow (OpenRouter/DeepSeek); uso > créditos → clamp 0; percent 420 → fração satura 1.0 (Grok/Cursor); `remainingFraction` negativo → 1.0 (Antigravity); aninhamento profundo (120 níveis) → `findQuotaInfo` mapeia sem crash; quota com total 0 → snapshot vazio HONESTO (não erro, não 0 fake); Cursor tolerante com tipos errados → snapshot sem janela (nada inventado). **P3 aceito:** saldo NEGATIVO do DeepSeek passa sem clamp (o valor exibido é o que a API disse; OpenRouter clampa) — Decisão 10 do `decisoes-f5.md` |
| 5 | **50 contas** | 🟢 DEFENDE | `fiftyAccountsInOneEvaluation`: 50 contas × janela num ÚNICO evaluate → 25 disparos (só as acima do threshold), sem duplicata por conta, ordem determinística (`AlertEngine.ordering`), 2º ciclo → dedupe total, restart → sem re-disparo. `registryWithFiftyOpenRouterKeys`: 50 keys registradas (account_key único, labels ordenados, isolamento por provider, toggle batch off → descoberta vazia, registros permanecem). E2E complementa com 31 contas CLAUDE ciclando dentro do orçamento (seções F4 mantidas) |
| 6 | **Headers Alibaba faltantes em runtime (degrada?)** | 🔧 FIXADO (carry-forward) | O port inicial omitia `Origin`/`Referer` que a referência manda no modo API key — **fixado**: `Origin` = gateway da região, `Referer` = `dashboardURL` da região (URLs bit-a-bit do `AlibabaCodingPlanUsageFetcher`). Contrato pinado em `AlibabaProviderTests` (headers conferidos no request real capturado). Degradação se o gateway rejeitar permanece honesta: 401/403 nas duas regiões → snapshot `.invalid` com badge (`unauthorizedOnBothRegionsDegrades`), nunca erro na barra |
| 7 | **Registration bypass overlap programático residual** | 🔧 FIXADO (fecha residual do F4) | O guard de overlap morava só na UI; INSERT/registro direto com dir sobre a raiz canônica DOBRAVA o histórico (documentado no F4 caso 5). **O ciclo agora defende**: `ProviderCoordinator.overlapsCanonical` (igual, descendente OU ancestral; fronteira de componente `/a/b` ≠ `/a/bc`; nil/dir vazia → false) e a conta sobreposta NÃO ingere (linha permanece no painel p/ remoção). `RedTeamF5UITests`: matriz de paths + ciclo com conta "Evil Overlap" (dir == raiz canônica) → corpus contado 1×, estável no 2º ciclo, irmã legítima continua somando. Supersede parcial da Decisão 7 do `decisoes-f4.md` (a dobragem entre contas IRMÃS com a mesma dir segue aceita para registro programático) |
| 8 | **Hermeticidade do e2e (achado de runtime do gate)** | 🔧 P1 DO GATE — FIXADO | Runs 1–3 do E2E v5 mostraram `U:19% K:11%` no menu bar com `authState: ok`: a máquina de execução tinha credenciais REAIS de Cursor (`state.vscdb`) e Grok (`~/.grok/auth.json`) e o app fez REQUEST REAL durante o e2e (violação do isolamento §9 — sem vazamento de credencial em evidência, mas com rede real). **Fix:** `TOKENBAR_CURSOR_DB`/`TOKENBAR_GROK_AUTH`/`TOKENBAR_ANTIGRAVITY_CREDS` apontam arquivo INEXISTENTE no lab; `DEEPSEEK_API_KEY`/`ALIBABA_*`/`DASHSCOPE_API_KEY` neutralizadas com vazio (blindagem contra env ambiente); check novo "texto é EXATAMENTE C X G Z O" pina a hermeticidade (run 4+: PASS) |

## Notas de método

- Os casos 1–2 são contratos POSITIVOS pinados (o motor se comporta exatamente como a spec §8 descreve) — a red team os ataca para medir o dano máximo e documentar; mudar o comportamento exigiria mudança de contrato com o usuário.
- Os casos hostis (4) usam fixtures sintéticas `fake-*` e hosts `.example.com` — nenhum endpoint real é tocado; o caso alibaba usa somente a região primária (o fallback aponta o host canônico compartilhado do stub global — contaminação entre suítes evitada por construção do teste).
- E2E: 8 checks F5 novos cobrem alerta disparado/dedupe/hermeticidade/credits no app real (log commitado).
